<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="14dp">

    <!-- Month navigation -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="8dp">

        <ImageView
            android:id="@+id/cal_prev"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:padding="6dp"
            android:src="@drawable/ic_widget_next"
            android:contentDescription="חודש קודם" />

        <TextView
            android:id="@+id/cal_month"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textColor="@color/widget_header"
            android:textSize="16sp"
            android:textStyle="bold"
            android:text="" />

        <ImageView
            android:id="@+id/cal_next"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:padding="6dp"
            android:src="@drawable/ic_widget_prev"
            android:contentDescription="חודש הבא" />
    </LinearLayout>

    <!-- Weekday headers (Sun→Sat, RTL so Sunday is on the right) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layoutDirection="rtl"
        android:paddingBottom="4dp">
        <TextView style="@style/CalHeader" android:text="א" />
        <TextView style="@style/CalHeader" android:text="ב" />
        <TextView style="@style/CalHeader" android:text="ג" />
        <TextView style="@style/CalHeader" android:text="ד" />
        <TextView style="@style/CalHeader" android:text="ה" />
        <TextView style="@style/CalHeader" android:text="ו" />
        <TextView style="@style/CalHeader" android:text="ש" />
    </LinearLayout>

    <GridView
        android:id="@+id/cal_grid"
        android:layout_width="match_parent"
        android:layout_height="216dp"
        android:numColumns="7"
        android:layoutDirection="rtl"
        android:stretchMode="columnWidth"
        android:gravity="center"
        android:scrollbars="none" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="תזכורות החודש"
        android:textColor="@color/widget_sub"
        android:textSize="12sp"
        android:textStyle="bold"
        android:paddingTop="8dp"
        android:paddingBottom="6dp" />

    <ListView
        android:id="@+id/cal_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:scrollbars="none" />

    <TextView
        android:id="@+id/cal_list_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:textColor="@color/widget_sub"
        android:textSize="13sp"
        android:visibility="gone"
        android:text="אין תזכורות החודש" />
</LinearLayout>
